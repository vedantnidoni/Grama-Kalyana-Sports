<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GramaKalyanaSports">
        <activity android:name=".TournamentSetupActivity" />
        <activity android:name=".TournamentSetupActivity" />
        <activity android:name=".ScorerActivity" />
        <activity android:name=".FanViewActivity" />
        <activity android:name=".PlayerStatsActivity" />
        <activity android:name=".ScorecardActivity" />
        <activity android:name=".CricketScorerActivity" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.GramaKalyanaSports">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>